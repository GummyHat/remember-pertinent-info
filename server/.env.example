# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=remember_pertinent_info
DB_USER=postgres
DB_PASSWORD=your_password_here

# Server Configuration
PORT=3001

# CORS Configuration (Security)
# In production, set this to your frontend domain (e.g., https://yourapp.com)
# In development, leave unset to allow all origins
# CORS_ORIGIN=http://localhost:8000

# Environment
# NODE_ENV=production  # Set in production for strict CORS
